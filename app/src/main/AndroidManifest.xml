<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xmlkotlinflow">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.XmlKotlinFlow">
        <activity android:name=".ui.CountDownStateFlowActivity"
            android:exported="false" />
        <activity android:name=".ui.ButtonIncLiveDataActivity"
            android:exported="false" />
        <activity android:name=".ui.ButtonIncStateFlowActivity"
            android:exported="false" />
        <activity android:name=".ui.CountDownLiveDataActivity"
            android:exported="false" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>